<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :pp %><br>
    <%= f.file_field :pp %>
  </div>

  <div class="field">
    <%= f.label :gender %><br>
    <%#= f.collection_select :gender ,  User::GENDER_TYPES %>
  <%#= f.collection_select :gender, User.order(:name),:id,:name, include_blank: true %> 
    <%= f.select(:gender, ['male','female']) %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
